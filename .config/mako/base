# global configuration 
max-visible=4
sort=-time
anchor=top-right
default-timeout = 10000
# ignore-timeout 0|1 ignore expire timeout and use default-timeout instead.

# touch/button
on-touch=dismiss

# style options are joined in via base16 manager to create config
